import { Component } from 'react';
export interface ModifiersProps {
    active: number;
    onChange: (modifier: number) => void;
}
export default class Modifiers extends Component<ModifiersProps> {
    handleModifierClick: (modifier: number) => void;
    render(): JSX.Element;
}
